# 📋 Telegram Task-Board Mini App

Telegram Mini App для управления задачами в формате канбан-досок. Полнофункциональное приложение для создания досок, колонок и карточек задач прямо внутри Telegram.

## 🚀 Возможности

- ✅ **Управление досками задач** - создание и организация канбан-досок
- ✅ **Drag-and-Drop** - интуитивное перемещение карточек между колонками
- ✅ **Совместная работа** - приглашение пользователей на доски
- ✅ **Чек-листы и комментарии** - детальное управление задачами
- ✅ **Интеграция с Telegram** - авторизация через Telegram Mini App API
- ✅ **Система подписок** - интеграция с YooKassa для премиум-функций
- ✅ **RESTful API** - полный набор эндпоинтов для всех операций
- ✅ **Swagger документация** - автоматическая документация API
- ✅ **Адаптивный дизайн** - работа на мобильных устройствах и десктопе

## 🛠 Технологии

### Frontend
- **React 18** + **TypeScript** - современный UI фреймворк
- **Vite** - быстрый сборщик
- **Tailwind CSS** - utility-first CSS фреймворк
- **@dnd-kit** / **@hello-pangea/dnd** - drag-and-drop функциональность
- **Lucide React** - иконки

### Backend
- **Node.js** + **Express.js** - серверная платформа
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация и авторизация
- **Swagger** - автоматическая документация API
- **@yookassa/sdk** - интеграция платежной системы

### Тестирование
- **Jest** - unit и интеграционные тесты
- **Locust** - нагрузочное тестирование

## 📦 Установка

### Требования

- Node.js >= 18.0.0
- PostgreSQL >= 12.0
- npm или yarn

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/your-username/telegram-trello-mini-app.git
cd telegram-trello-mini-app
```

### Шаг 2: Установка зависимостей

**Frontend:**
```bash
npm install
```

**Backend:**
```bash
cd backend
npm install
```

### Шаг 3: Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE trello_db;
```

2. Запустите миграции:
```bash
cd backend
psql -U postgres -d trello_db -f schema.sql
```

Или используйте скрипт:
```bash
node update_database.js
```

### Шаг 4: Настройка переменных окружения

Создайте файл `backend/.env`:

```env
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/trello_db

# Server
PORT=3001
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-key-here

# CORS
CORS_ORIGIN=http://localhost:5173

# YooKassa (опционально)
YOOKASSA_SHOP_ID=your-shop-id
YOOKASSA_SECRET_KEY=your-secret-key
YOOKASSA_RETURN_URL=https://your-domain.com/payment-success
```

### Шаг 5: Запуск приложения

**Backend (в отдельном терминале):**
```bash
cd backend
npm run dev
```

Backend будет доступен на `http://localhost:3001`

**Frontend (в отдельном терминале):**
```bash
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## 📖 Использование

### Для разработки

1. Запустите backend и frontend (см. выше)
2. Откройте приложение в браузере: `http://localhost:5173`
3. Для работы в Telegram Mini App настройте бота через [@BotFather](https://t.me/BotFather)

### API Документация

После запуска backend, Swagger документация доступна по адресу:
- **Swagger UI**: `http://localhost:3001/api-docs`
- **OpenAPI JSON**: `http://localhost:3001/api-docs.json`

### Основные эндпоинты

- `POST /api/telegram/register` - регистрация через Telegram
- `GET /api/boards` - список досок пользователя
- `POST /api/boards` - создание доски
- `GET /api/columns?board_id={id}` - список колонок доски
- `POST /api/cards` - создание карточки
- `PUT /api/cards/:id/move` - перемещение карточки
- `GET /api/subscriptions/me` - информация о подписке

Полный список эндпоинтов см. в Swagger документации.

## 🧪 Тестирование

### Unit тесты (Jest)

```bash
cd backend
npm test
```

Запуск конкретных тестов:
```bash
npm run test:user      # Тесты пользователей
npm run test:board    # Тесты досок
npm run test:card     # Тесты карточек
npm run test:column   # Тесты колонок
```

### Нагрузочное тестирование (Locust)

```bash
cd backend/tests/load

# Установка зависимостей
pip install -r requirements.txt

# Запуск веб-интерфейса
locust -f locustfile.py --host=http://localhost:3001

# Или автоматический тест
python run_load_test.py
```

Подробнее см. [backend/tests/load/README.md](backend/tests/load/README.md)

## 📁 Структура проекта

```
telegram-trello-mini-app/
├── backend/                 # Backend приложение
│   ├── src/
│   │   ├── database/       # Подключение к БД и ORM
│   │   ├── routes/         # API маршруты
│   │   └── middleware/     # Middleware (auth, access control)
│   ├── tests/              # Тесты
│   │   ├── orm.*.test.js   # Unit тесты
│   │   └── load/           # Нагрузочное тестирование
│   ├── schema.sql          # Схема базы данных
│   └── server.js           # Точка входа сервера
│
├── src/                    # Frontend приложение
│   ├── components/         # React компоненты
│   ├── hooks/              # Custom hooks
│   ├── utils/              # Утилиты
│   └── types/              # TypeScript типы
│
├── public/                 # Статические файлы
└── dist/                   # Собранное приложение
```

## 🗄 База данных

Проект использует PostgreSQL с 8 основными сущностями:

- **users** - пользователи системы
- **boards** - доски задач
- **columns** - колонки в досках
- **cards** - карточки задач
- **checklist_items** - элементы чек-листов
- **comments** - комментарии к карточкам
- **subscriptions** - подписки пользователей
- **board_members** - участники досок

Схема базы данных: [backend/schema.sql](backend/schema.sql)

## 🔐 Безопасность

- JWT токены для аутентификации
- Валидация входных данных на сервере
- Защита от SQL-инъекций через параметризованные запросы
- CORS настройки для безопасного взаимодействия
- Проверка прав доступа к ресурсам

## 🚀 Деплой

### Production сборка

**Frontend:**
```bash
npm run build
```

Собранные файлы будут в папке `dist/`

**Backend:**
```bash
cd backend
NODE_ENV=production npm start
```

### Переменные окружения для production

Убедитесь, что установлены все необходимые переменные окружения в `.env` файле, особенно:
- `DATABASE_URL` - строка подключения к production БД
- `JWT_SECRET` - надежный секретный ключ
- `CORS_ORIGIN` - домен вашего frontend
- `YOOKASSA_*` - ключи для платежной системы

## 📝 Скрипты

### Frontend
- `npm run dev` - запуск dev сервера
- `npm run build` - production сборка
- `npm run preview` - предпросмотр production сборки
- `npm run lint` - проверка кода

### Backend
- `npm start` - запуск production сервера
- `npm run dev` - запуск с nodemon (автоперезагрузка)
- `npm test` - запуск всех тестов
- `npm run test:watch` - тесты в watch режиме

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан в учебных целях.

## 👥 Команда

- Хусаинов Тимур - Backend Developer
- Кинзебаев Тагир - Frontend Developer
- Герасименко Никита - Аnalyst

## 🔗 Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [YooKassa API Documentation](https://yookassa.ru/developers/api)
- [Swagger Documentation](https://swagger.io/docs)
- [React Documentation](https://react.dev)
- [Express.js Documentation](https://expressjs.com)



